```javascript

// Flows 입력값
// User ID: {{context.user.user_id}}
// Body
{
  "user_metadata": {
    "company_name": "{{fields.company_name_input}}"
  }
}

// Action 코드
// 제목: Render Progressive Profile Form
exports.onExecutePostLogin = async (event, api) => {
  const FORM_ID = 'ap_hkXD1yFucDid9gVLtLMrBN';

  const userMeta = event.user.user_metadata || {};
  const companyName = userMeta.company_name;

  if (event.stats.logins_count > 2 && !companyName) {
    return api.prompt.render(FORM_ID);
  }
};

exports.onContinuePostLogin = async function(event, api) {
  return;
};
